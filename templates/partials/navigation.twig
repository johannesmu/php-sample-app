<header class="navbar navbar-expand-lg justify-content-between navbar-dark bg-dark">
  <button class="navbar-toggler order-1  mx-0" type="button" data-toggle="collapse" data-target="#nav-collapse">
    <span class="navbar-toggler-icon"></span>
  </button>
  <a class="navbar-brand order-3 order-sm-2 order-md-2 ml-3" href="./">&</a>
  <form class="form-inline ml-md-auto flex-fill order-2 order-sm-2">
    <div class="input-group flex-fill">
      <input class="form-control" type="search" name="search" placeholder="search">
      <div class="input-group-append">
        <button class="btn btn-secondary" type="submit"> <i class="fas fa-search"></i> </button>
      </div>
    </div>
  </form>
  <nav class="nav shopping ml-auto ml-md-auto align-self-end order-4 order-sm-4">
    <a class="nav-link text-light" href="wishlist.php"> 
      <i class="fas fa-heart"></i>
      {% set wishlist_total = (wish_total) ? wish_total : 0 %}
      <span class="badge badge-secondary">{{wishlist_total}}</span>
    </a>
    <a class="nav-link text-light" href="shoppingcart.php"> 
      <i class="fas fa-shopping-cart"></i> 
      <span class="badge badge-secondary">0</span> 
    </a>
  </nav>
  <div class="collapse navbar-collapse flex-grow-0 flex-shrink-1 order-6 order-sm-6" id="nav-collapse">
    <nav class="navbar-nav">
      {% for item in navigation.items %}
        {% set active = (item.link == navigation.active) ? 'active' : '' %}
        <a class="nav-link {{active}}" href="{{item.link}}">{{item.name}}</a>
      {% endfor %}
    </nav>
  </div>
</header>
